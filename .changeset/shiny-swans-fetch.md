---
'unutils': minor
---

Added the following new utilities from [`nanoid`](https://npmx.dev/nanoid) package, belonging to the new `id` group:

- `nanoid`
- `nanoidFactory`
- `NANOID_URL_ALPHABET` (constant)